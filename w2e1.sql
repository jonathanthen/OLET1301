/*SELECT COUNT(*)
 FROM Cities;*/

SELECT AVG(wind_speed)
  FROM Observations;

SELECT COUNT(city)
  FROM Observations;

SELECT COUNT(DISTINCT city)
  FROM Observations;
  
SELECT MIN(wind_speed)
  FROM Observations;

SELECT MAX(wind_speed)
  FROM Observations;

SELECT MAX(wind_speed) FROM Observations;

SELECT MIN(wind_speed) FROM Observations;

SELECT AVG(wind_speed) FROM Observations;

SELECT SUM(wind_speed) FROM Observations;