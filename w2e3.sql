SELECT MIN(wind_speed),
       MAX(wind_speed),
       AVG(wind_speed),
       MIN(air_temp),
       MAX(air_temp),
       AVG(air_temp)
  FROM Observations;

SELECT MIN(wind_speed),
       MAX(wind_speed),
       AVG(wind_speed),
       MIN(air_temp),
       MAX(air_temp),
       AVG(air_temp)
  FROM Observations;